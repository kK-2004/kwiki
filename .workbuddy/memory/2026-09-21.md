# 2026-09-21

## 任务：补齐全仓库注释中文化 + 新建 AGENT.md

用户两条指令：
1. 在项目根目录创建 `AGENT.md`，约束使用中文注释。
2. 检查注释、全部改成中文，已是中文的不修改。

### 结果

- **新建 `AGENT.md`**：写明注释语言规则、保留英文的例外清单、
  禁止翻译的「伪注释」形态、Flyway 迁移只读、无 Docker、密钥、
  验收命令、既有问题。

- **补译 22 个文件、51 行注释**。全仓库扫描 595 个待扫文件，
  仅剩 74 行英文注释，其中 26 个文件/101 行属于 Flyway 迁移目录（**刻意跳过**），
  再扣掉代码样例/HTML 标签/标识符等保留项，最终改 51 行。

### 新增/变化的仓库事实

- 仓库多了一个前端：`admin-frontend/`（2026-09-13 起）。
- Flyway 迁移已到 `V27`；`pom.xml` 的 content-center-sdk 版本升到 `0.1.4`。
- `pnpm` 不在 PATH，前端校验要用 `node_modules/.bin/vue-tsc`、`node_modules/.bin/vitest`。

### 验证结论

- 自写掩码校验器证明：51 行改动全部落在注释正文，
  注释标记、行尾代码、代码本体逐字符不变（22/22 文件通过）。
- `mvnw -o compile`、`mvnw -o test-compile` 退出码 0。
- `ArchitectureRulesTest` 13/13、`NoDockerMiddlewareAuditTest` 4/4、
  `RepositorySecretsAuditTest` 4/4 通过。
- `vue-tsc --noEmit` 退出码 0。
- 前端 `vitest`：94/96 通过。
- 残留英文扫描：23 条纯英文 + 7 条半中半英，**逐条确认均为刻意保留**
  （HTML 结构标签、魔法字节、缩写、被引用的英文报错原文、技术标识符）。
- `db/migration` 零改动（`git diff` 确认）。

### 与本次改动无关的既有失败（未修）

1. `NoMinioCouplingAuditTest.pomDeclaresOnlyTheContentCenterStorageDependency`
   —— pom.xml 已升到 0.1.4，测试仍硬编码 0.1.3。
   `git diff pom.xml` 显示该文件相对 HEAD 的唯一改动就是这一行版本号。
2. 前端 `archive-navigation.spec.ts`、`citation-focus.spec.ts`
   收集失败：`element-plus` 的 `theme-chalk/el-button.css` 报
   `Unknown file extension ".css"`；element-plus 在 HEAD 的 package.json 里就已存在。
3. 前端 `editor.spec.ts` 断言 `无法解析媒体地址` 失败
   —— 该字符串在 HEAD 的 `frontend/src` 里同样不存在。

### 工具经验

- **沙箱文件读取代理会拦截「敏感内容」**：批量扫描到某个文件时
  抛 `PermissionError` 并终止**整个进程组**（外层 bash 也死、输出全丢）。
  解法：分块扫描（每块 60 文件、独立进程）+ 进度落盘，再单独读日志。
- `SecretRedactionTest.java` 因含疑似密钥样例被沙箱拦截，
  用户选择不共享内容 → 该文件本轮未检查、未改动。
