# 2026-09-10

## 全仓库注释中文化

- 需求：把 kwiki 项目所有英文代码注释改成中文。
- 规模：**315 个文件**（265 java / 19 sql / 12 vue / 12 ts / 4 sh / 2 yml / 1 css）+ `pom.xml`，
  约 1600 行注释。改动 `1618 insertions(+) / 1666 deletions(-)`。
- 做法：写了「注释抽取 → 译文映射 → 脚本落地 → 独立验证」工具链（工作副本在
  `.workbuddy/tmp-i18n/`，已清理）。前 7 个模块由子代理并行完成（它们虽报 429/502
  但实际仍在后台跑完），剩余 959 行自行处理。
- 关键保障：**剥离注释后逐行比对**，316 个文件代码 byte-identical，仅
  `frontend/pnpm-lock.yaml` 为本次改动前就存在的差异。
- 验证通过：`mvnw -o compile` / `test-compile` 均 EXIT=0；前端 `vue-tsc --noEmit` EXIT=0；
  `*AuditTest` + `ArchitectureRulesTest` 共 20 个用例全绿。
- 发现的既有问题（**与本次改动无关**）：`CommonResponseContractTest` 因
  `WikiMockBeans` 未提供 `WikiPageDraftRepository` bean 导致 Spring 上下文加载失败
  （HEAD 同样缺失）。后续若要修，补这个 mock 即可。
- 例外保留：`(.gitignore) ---- IDE ----`、`mediaBlocks.ts` 的序列化语法样例、
  `validate-migrations.sh` 的命令示例、`MediaContentSniffer` 的魔法字节标注
  —— 属代码样例，不翻译。
