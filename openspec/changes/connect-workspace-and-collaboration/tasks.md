## 1. 实施准备与离线契约

- [x] 1.1 复核当前工作区、已有未归档 Wiki/内容中心/Agentic 变更与 SSE 接口，建立本变更触及的接口/数据出口清单，保留已有未提交实现。
- [x] 1.2 固化 auth、分页、资源授权、会话、导入、协作、划词、互动、通知的请求/响应 DTO 和错误码；补 TransDTO 与扁平 SSE 示例。
- [x] 1.3 枚举全部 Flyway 迁移及未跟踪文件，确认 MySQL、V<N>__*.sql 和当前最大版本；增加只读离线版本/命名/重复检测及有效/无效 fixture 验证，不修改 V1–V5。
- [x] 1.4 建立显式离线测试入口/白名单，隔离会连接 MySQL 或执行 Flyway clean 的测试；HTTP、模型、存储、Redis 使用边界替身，不启动 MySQL 或其他外部服务。

## 2. 账号认证与前端登录态

- [x] 2.1 实现注册服务/API：标准化、唯一冲突映射、BCrypt、活跃状态和可写字段白名单，禁止客户端授予管理员。
- [x] 2.2 修改 JwtTokenService 为 ID-only sub+iat+exp，按 ID 从数据库加载 CurrentUser 和当前平台权限；拒绝旧格式和禁用用户，保留异步 SSE dispatch 认证兼容。
- [x] 2.3 使用可注入 Clock 实现 7 天有效、5 天内续签及 refresh 契约，支持 API/SSE 响应头携带更新 token，排除后台轮询/心跳续签。
- [x] 2.4 实现 /auth/me、注册/登录错误契约与缺失数据库的明确失败；配置所需公开认证路径和跨源续签头暴露。
- [x] 2.5 建立统一 auth store、sessionStorage、单调 iat 更新和 auth generation，退出清理数据与流，认证守卫保留本站 returnTo/邀请上下文。
- [x] 2.6 设计实现登录/注册页面，包含表单校验、密码显隐、重复提交保护、错误和登录后恢复位置。
- [x] 2.7 离线验证 t0+4d、精确 5d、5d+1s、7d、失效令牌、权限即时撤销、乱序响应、退出后的迟到响应、后台不续签和 SSE 续签头。

## 3. 数据增量与统一资源权限

- [x] 3.1 实施前重新分配迁移版本，追加 owner_id、审核开关、audience_mode、ADMIN、page_member/page_audience_member、范围版本和管理审计结构；规划历史数据回填和索引，不执行真实库迁移。
- [x] 3.2 实现 owner_id 与 created_by 分离、唯一 owner/OWNER 映射校验以及 ResourceAuthorizationService 的 READ/EDIT/MANAGE/TRANSFER 矩阵。
- [x] 3.3 实现 PRIVATE/SELECTED_MEMBERS/KB_MEMBERS 和文档/知识库管理继承，直接授权与来源知识库成员授权合并，来源退出失效。
- [x] 3.4 实现可浏览知识库树和权限范围内成员搜索 API、受众变更 API，提交时复核来源成员；PRIVATE 切换原子撤销普通授权/邀请/申请并返回影响范围。
- [x] 3.5 将既有 Wiki 树、页面/草稿/修订、来源/附件下载、反链/摘要和管理接口切换为资源级授权，禁止 shared page 扩大为全库权限。
- [x] 3.6 扩展 AuthorizationScope、范围版本失效和 ES 过滤，覆盖仅共享单文档、私有页以及出站模型上下文/引用的再次授权。
- [ ] 3.7 离线验证完整角色矩阵、跨知识库受众、管理继承、退出来源库、PRIVATE 切换、无权祖先元信息及流中权限撤销。

## 4. API 客户端与全页面骨架

- [x] 4.1 统一 API client 解包 TransDTO、HTTP/业务错误、PATCH/DELETE、FormData、分页、AbortSignal 和 token 续签；保留 SSE 独立解码。
- [x] 4.2 拆分全局 AppShell/WikiLayout，建立 design.md 路由清单、旧数字 Wiki URL 重定向、首页和 404；实现侧栏选中与认证页隔离。
- [x] 4.3 实现真实知识库列表/创建/切换、Wiki 元信息/阅读/草稿/发布/历史与树操作，接通索引状态和归档入口。
- [x] 4.4 按新版本迁移追加 agent_definition/recent_visit/page_summary 及 repository/API，智能体记录只初始化真实系统能力，最近访问和摘要不使用示例内容。
- [x] 4.5 实现智能体列表/详情/开始会话、全局搜索、最近访问和摘要视图，替换固定文档/账号/面包屑/数字。
- [x] 4.6 遍历现有可点控制形成验收清单，补加载/空/错误/无权/重试与陈旧请求取消，权限不足隐藏管理入口。
- [x] 4.7 用离线 API fixtures 验证 envelope、导航、旧链接、空库、后端故障、快速切页和键盘操作；fixture 不进入生产模块。

## 5. 持久 Agentic 会话与聊天浮窗

- [x] 5.1 追加 chat_session/chat_message 扩展和 chat_run 迁移，包含 owner、逻辑删除、状态、clientMessageId 唯一、seq/引用及安全进度元信息。
- [x] 5.2 实现本人会话创建/列表/详情/重命名/删除 API，历史消息/引用授权过滤和有界上下文构建。
- [x] 5.3 扩展 /chat/stream 的 sessionId/clientMessageId/agentId 与首个 session 事件，保留 query-only 兼容；生成前落用户消息/run，防重复和同会话并发运行。
- [x] 5.4 将业务消息终态持久化与已有审计分离，保存完成/失败/取消/部分答案，补重启 RUNNING→INTERRUPTED 和保存失败反馈。
- [x] 5.5 实现全局 conversation store 的连接所有权、取消/错误/终态和页面挂接，禁止组件卸载误取消或重复生成。
- [x] 5.6 实现新聊天输入→右下角浮窗→最小化按钮→恢复→最大化会话页，以及会话列表、历史、续聊、重命名、删除和停止。
- [x] 5.7 扩展工具/QA/公开解释事件字段、真实分数类型和安全摘要，保持 requestId/seq/一次终态；禁止透传隐藏推理和原始敏感参数。
- [x] 5.8 实现工具状态摘要列表、QA 原因/重试、单行最新解释/展开区域、未知事件兼容及两层独立 followTail，支持回到底部。
- [ ] 5.9 验证重复发送、session 越权、历史失权遮蔽、生成中最小化/最大化单连接、取消/中断/落库失败、事件乱序和用户上滚不抢滚动条。

## 6. DOCX/Markdown 导入成 Wiki

- [x] 6.1 追加 wiki_import_job 迁移，记录幂等键、附件/页面、任务状态/租约/尝试次数和失败原因；增加任务查询与重试 API。
- [x] 6.2 实现新建上传模态和二级受众选择窗口，左侧知识库树、右侧用户名搜索/多选/移除，保留跨库选择并明确仅自己管理例外。
- [x] 6.3 在内容中心上传边界增加导入用途和 DOCX/MD 扩展名/签名/MIME/大小校验，处理通用 Markdown MIME、危险 DOCX 解包和嵌入图片提示。
- [x] 6.4 复用现有解析器增加规范 Markdown 转换，保留文本结构并净化；实现可恢复解析 worker。
- [x] 6.5 事务创建页面、首修订/发布、来源、受众与索引任务，提交前复核权限，使用幂等关联避免重复页面；Wiki 导入附件不重复进入检索索引。
- [x] 6.6 接入上传/解析/成功 Wiki 跳转与独立索引状态，失败显示真实原因和适用的重试入口。
- [ ] 6.7 使用本地 DOCX/MD fixture 和存储/索引替身验证正常结构、非法文件、超限、重复提交、worker 重试、受众变化和解析成功但索引失败。

## 7. 邀请协作与所有权

- [x] 7.1 追加邀请 token hash、join_request、ownership_transfer 的迁移及去重/期限/状态索引。
- [x] 7.2 实现知识库/文档邀请创建/列表/复制/撤销/预览/接受，默认 7 天且 1h–30d 可配，仅 VIEWER/EDITOR，复核资源和发行者权限。
- [x] 7.3 实现审核开关与申请列表/批准/拒绝，按当前角色/期限/状态原子审批，处理重复接受/审批和开关变化，不降级已有成员。
- [x] 7.4 实现创作者任免直接 ADMIN、继承管理员展示及来源撤销规则，阻止普通管理员任免管理员或转交。
- [x] 7.5 实现所有权转交发起/撤销/接收确认和 CAS，原 owner 转 EDITOR，保留历史作者，更新范围版本并审计。
- [x] 7.6 完成资源协作模态、邀请登录落地、成员/审核/转交页面和共享空间真实列表。
- [ ] 7.7 验证链接过期/撤销/发行者失权、无需审核直接加入、审核中权限变化、重复与并发审批、单页邀请不扩权以及并发转交唯一 owner。

## 8. 评论与划词 AI

- [x] 8.1 追加 selection_anchor/wiki_comment 的迁移与根/回复/软删除查询索引，统一 UTF-16 offset 和版本锚点格式。
- [x] 8.2 实现页面评论/回复列表与创建 API，后端推导 parentId/replyTo，根锁串行化，验证跨页、删除和锚点有效性；写接口预留幂等键。
- [x] 8.3 在阅读器实现稳定段落标识、单段划词工具栏、评论输入和版本锚点定位，跨段选择明确提示。
- [x] 8.4 新增独立 SelectionQuestionLlmClient 与流式 API，读取真实标题/元信息/完整段/划词/query，区分文档数据与指令，支持预算/取消/错误/权限复核。
- [x] 8.5 实现划词问 AI 输入与答案流、引用上下文、停止和重试，保持与 Agentic 会话状态隔离。
- [x] 8.6 实现锚点跨版本唯一匹配与失配历史回退，验证重复词、文本删除、UTF-16、多版本和伪造段落；用模型替身验证完整上下文与撤权取消。

## 9. @ 与消息中心

- [x] 9.1 追加 comment_mention/notification 迁移及 recipient/readAt 分页索引、评论/用户/类型去重。
- [x] 9.2 实现权限内候选搜索、完全/前缀/包含排序、稳定分页与 LIKE 转义；提交时再次检查收件人并与评论同事务写通知。
- [x] 9.3 实现 @ 输入 token、200ms 防抖/取消、方向键/Enter/Escape/鼠标、多用户选择和失效候选修正。
- [x] 9.4 实现本人通知列表/未读数/单条和全部已读，过滤失权/隐藏评论，badge 口径与列表一致。
- [x] 9.5 实现侧栏 badge、右侧消息中心、已读状态、Wiki 锚点/评论深链及失效目标提示；可见轮询/聚焦刷新不续签 JWT。
- [ ] 9.6 验证候选权限与排序、乱序响应、多选、伪造收件人、事务回滚/重试去重、未读幂等和失权消息无内容泄露。

## 10. 点赞收藏与评论删除策略

- [x] 10.1 追加 page_like/page_favorite/comment_like 迁移和资源+用户唯一关系、个人时间分页索引。
- [x] 10.2 实现文档/评论点赞取消、文档收藏取消幂等 API 与用户信息/点赞/收藏列表 API，逐项执行资源可读检查。
- [x] 10.3 实现评论作者/管理者单条逻辑删除、根删除即时隐藏整组、非根删除占位引用以及所有分页/深链的根可见性过滤。
- [x] 10.4 实现可见评论聚合及根删除一次 N+1 扣减所需 delta，根锁协调回复与删除，避免同步修改全部回复。
- [x] 10.5 定义 CommentCleanupStrategy/CommentCleanupTrigger，实现 Asia/Shanghai 每日 01:00 分批逻辑清理、集群锁、游标/补跑/失败指标，为 XXL-JOB 保留替换触发器。
- [x] 10.6 实现标题下互动统计区、树形评论/回复/点赞/删除、个人主页点赞收藏列表及跳转。
- [ ] 10.7 验证双重点赞取消、回复的 parentId/replyTo、跨页拒绝、根/非根删除、分页隐藏、根删除竞态及清理重复运行不二次扣数。

## 11. Redis 统计同步与修复

- [x] 11.1 追加 stats_revision/stats_repair 迁移及资源唯一/待修复索引；定义文档 likes/favorites/comments 与评论 likes 的真实数据库聚合口径。
- [x] 11.2 确认 kk-common Redis/锁 API 与数值序列化，实现资源 key、完整 hash+版本、HINCRBY/EXPIRE 原子脚本和 30min TTL，禁止 JSON 数值直接 INCR。
- [x] 11.3 实现命中直接读、未命中锁/二查/一致快照回填、有界等待和无锁只读 DB 降级，查询命中不续 TTL。
- [ ] 11.4 为互动写和回填建立相同资源锁顺序/所有权/续租，将事务提交、版本连续增量、key 缺失完整回填与 delta=0 touch 接入所有互动操作。
- [x] 11.5 实现 Redis 失败返回业务成功/DB 统计、尽力失效和持久化合并修复；同步成功确认修复、默认分钟级重建，不重放不确定增量。
- [x] 11.6 将统计 API 和前端显示接入，业务失败回滚乐观状态，缓存失败不误导用户重复写；个人状态与全局计数分开处理。
- [ ] 11.7 离线验证 key 过期与写并发、多个 miss、重复/乱序版本、锁丢失、Redis 超时、回滚、幂等、TTL、根删除后回填和旧修复不覆盖新值。

## 12. 完整离线验收与交付

- [x] 12.1 按十份 spec 逐项对照验收记录，确认所有侧栏/树/账号入口有实际页面与操作，生产模块没有 fixture/mock 业务兜底。
- [x] 12.2 执行前端 typecheck、Vitest、build 和离线 Java 白名单测试，覆盖登录→上传 Wiki→邀请→划词 @→消息跳转→互动，以及浮窗→会话续聊；不连接真实 MySQL。
- [ ] 12.3 对固定聊天/成员窗口、键盘多选、长文本、两层滚动、窄屏和空/错误状态做浏览器交互验收，记录实际结果。
- [x] 12.4 运行只读迁移结构校验与 OpenSpec strict 校验，检查新增迁移唯一性、权限出口遗漏、既有 SSE 契约兼容以及未提交用户改动保护。
- [x] 12.5 整理部署顺序、旧 JWT 重新登录、ACL 不可回退到旧服务、功能开关与缓存修复操作说明；明确 MySQL 方言/真实迁移/外部 SDK 连通验证尚未执行，不将其计为已通过。
