## 1. 契约与实施基线

- [x] 1.1 复核当前工作区、关联未归档变更和本提案覆盖关系，列出现有行为替换点，保留其他未提交工作。
- [x] 1.2 固化 queryRound/attemptStage、ChildEvidence、CandidateAnswer、Quality v2、ActivityEvent 数据契约及错误分类，补充接口示例。
- [x] 1.3 核对实际表/FK/无 FK 引用、附件共享与源文档归属，建立归档/恢复/物理清理依赖清单。
- [x] 1.4 验证内容中心 SDK 上传/短链接的媒体 Content-Type、Range、跨域能力；确定图片/音视频允许类型、大小和不支持格式反馈。
- [x] 1.5 评估并确定能保留 source ranges 的 Markdown 解析和媒体块编辑方案，以复杂源码 round-trip 样例验证后锁定依赖版本。

## 2. 数据迁移与生命周期基础

- [x] 2.1 枚举完整 tracked/untracked Flyway 集，为归档批次/项目、生命周期版本、媒体用途和页面修订媒体引用分配唯一后续 migration。
- [x] 2.2 增加到期/回收站分页/引用查询索引及约束；历史 ARCHIVED 无时间数据从回填时刻保留完整 7 天。
- [x] 2.3 运行现有 migration 只读校验，增加新结构与历史数据升级的隔离 MySQL 契约验证，不更改旧 migration。
- [x] 2.4 实现统一生命周期状态和版本仓储、幂等归档批次、索引同步状态、可靠删除/重建任务。
- [x] 2.5 实现可用于已归档资源的回收站管理授权，验证整个子树范围，保留现有成员与资源权限约束。

## 3. 即时归档隔离与索引一致性

- [x] 3.1 实现 ResourceArchiveService，统一页面详情、Wiki 树和知识库旧归档入口，事务保存精确批次与 priorState。
- [x] 3.2 将生命周期版本/ACTIVE 过滤接入 ES 两路 pre-TopK、工作区搜索、父回取、引用和输出边界，处理缓存失效与过大过滤集合。
- [x] 3.3 实现页面/独占源资源与知识库范围的同步 ES 父子 chunks 删除，检查失败/冲突/超时并 refresh；成功返回 SYNCED。
- [x] 3.4 实现 ES 故障时 202/PENDING 返回、前端可读状态及可靠重试，证明索引清理未完成时内容仍不可检索。
- [x] 3.5 为归档/恢复/index worker 增加共享互斥与生命周期版本 fencing，防止旧 upsert 复活和旧 delete 误删恢复索引。
- [x] 3.6 更新 ES 文档/mapping 与可检索版本校验，恢复时重建当前已发布内容、草稿不入索引，屏蔽重建期间旧文档。
- [x] 3.7 用故障注入与并发测试覆盖归档期间双路检索、缓存陈旧、进行中回答、延迟 upsert/delete 和知识库级清理范围。

## 4. 回收站恢复与每日清理

- [x] 4.1 实现分页回收站查询与恢复接口，返回类型/标题/操作者/归档时间/到期时间/同步状态和明确 409/410 错误。
- [x] 4.2 实现批次恢复、原父失效时根目录恢复、知识库先恢复约束，保持此前独立归档项目的状态和到期时间。
- [x] 4.3 实现每天 Asia/Shanghai 01:00 的独立清理任务、固定 cutoff、多实例可靠锁与默认 200 项分批扫描。
- [x] 4.4 按依赖清单物理清理超过 168h 的本地数据，复验版本/到期/ES 删除，失败可重试且不会被游标永久跳过。
- [x] 4.5 保护共享附件/有效对象/会话快照，清理失效引用与子批次关系，不调用内容中心不存在的删除接口。
- [x] 4.6 添加 INFO 开始/批次/结束日志和计数、耗时、失败分类，验证零记录日志。
- [x] 4.7 用可控 Clock 和隔离数据库覆盖未满/恰好/超过 7 天、恢复与清理竞争、多实例、部分失败与历史回填。

## 5. 双路子检索与证据分层

- [x] 5.1 拆分 HybridRetrievalOrchestrator 的子 chunk 召回与 ParentEvidenceResolver 回取，子结果携带正文、修订和两路来源。
- [x] 5.2 将主问答检索固定为 BM25 + vector 两路独立排名，每次 RRF 常数 60、chunkKey 去重、稳定 tie-break，停止跨轮旧排名加分。
- [x] 5.3 引入基础分支 20/融合 8、扩大分支 50/融合 20 的配置与校验；正确区分候选数、最终 TopK 和实际上下文数量。
- [x] 5.4 实现子/父上下文预算、引用映射和父去重上限 8/16；首个 QA 前不读取父正文。
- [x] 5.5 保留单路失败降级、双路失败错误、成功零命中区别及 run 内 embedding 复用，所有路径使用相同权限过滤。
- [x] 5.6 增加真实排名/RRF 数值、扩检重算、未授权命中、引用区间与上下文裁剪的检索契约测试。

## 6. 预生成回答 QA 与有界反馈改写

- [x] 6.1 新增受限候选生成收集与 CandidateAnswer，不发送未通过的 token/citations，不写入最终会话回答。
- [x] 6.2 实现 Quality v2 schema/port/adapter，输入原始问题、当前 query、实际候选与证据；校验三项评分、0.80 阈值和引用/无依据陈述。
- [x] 6.3 将主 LangGraph 流程改为基础子 → 基础父 → 扩大子 → 扩大父，每次候选重新 QA，通过即停止，空证据/无新父上下文显式 skipped。
- [x] 6.4 实现 Query Rewrite Agent 的结构化反馈请求，保留原始 query、全部历史改写、上轮 query、阶段拒绝原因、父补充与 TopK 扩大事实。
- [x] 6.5 实现最大 3 query 轮/2 改写调用、重复/无效改写计数与不重复检索，新 query 重置基础 TopK。
- [x] 6.6 调整 AgenticLimits、RunContext、properties/schema 的模型/工具/步骤上限与默认 300s 时限，覆盖完整 27 次模型最坏路径，保留统一取消和有界缓冲。
- [x] 6.7 QA 通过后只发布被评审候选原文并持久化；内容补救耗尽输出精确拒答和空引用，基础设施/超时/取消使用独立终态。
- [x] 6.8 增加可脚本化模型/QA 测试：每个阶段通过、三轮全部失败、QA schema 无效、改写上下文完整与注入隔离、候选不泄漏、取消无后续调用。

## 7. 全链路 DEBUG 与活动事件

- [x] 7.1 引入共享阶段事件标识和 DEBUG 记录器，覆盖 route、两路召回、RRF、生成、QA、父回取、扩检、改写和终态。
- [x] 7.2 记录完整 query 身份/改写历史、分支排名与 TopK、候选 ID/摘要、门控阈值/拒绝原因、动作和预算；分离 RRF 与 QA 字段。
- [x] 7.3 复用 SecretRedaction、增加长度/截断标记、DEBUG 关闭快捷路径及异步 MDC/RunContext 绑定清理。
- [x] 7.4 产出 versioned activity SSE，包含稳定 stepId、queryRound、attemptStage、状态和真实指标，不发送内部候选/prompt/密钥。
- [x] 7.5 扩展会话事件持久化与重放；前端 reducer 按 seq 去重、stepId 合并、防终态回退，兼容旧事件与未知字段。
- [x] 7.6 验证跨线程日志串联/并发不串请求/敏感内容脱敏，以及 SSE 乱序、重复、重放和历史兼容。

## 8. 原型活动界面与回收站界面

- [x] 8.1 创建共用 RetrievalActivity，默认单行折叠，使用原型绿色图标、细时间线、弱化统计及完成/运行/失败视觉。
- [x] 8.2 展开双路分支、RRF/TopK、预生成/QA 和补救轮次，加入真实来源、长 query 展开、移动端与键盘支持。
- [x] 8.3 在 ConversationMessages 与 AgenticAnswerPanel 接入组件，消除 started/route/rewrite 原始状态展示，保留手动展开和滚动位置。
- [x] 8.4 全部归档入口接入两个连续确认弹窗、最终提交防抖/幂等和 pending 状态，任一步取消不写入。
- [x] 8.5 增加工作区/知识库回收站路由、分页列表、到期信息、恢复动作与索引重建提示，接入权限和冲突反馈。
- [x] 8.6 验证原型布局、窄屏、键盘操作、完整三轮轨迹、历史消息、两次确认取消和回收站恢复交互。

## 9. 媒体服务与可视 Markdown 编辑

- [x] 9.1 扩展附件实际内容/MIME 校验和 SDK 上传返回值，统一图片专属索引资格；只有图片进入文档索引，其他附件仅页面展示，持久化页面修订媒体引用。
- [x] 9.2 实现稳定 attachment 引用的授权预览与短链刷新，校验页面/修订引用和生命周期，处理 Range/播放器错误。
- [x] 9.3 升级共享 Markdown 解析/安全渲染，支持标准图片、受限 img/audio/video 属性及内部引用，覆盖代码、括号、转义和恶意输入。
- [x] 9.4 升级 MarkdownEditorAdapter 为源码可映射的媒体块编辑视图，文本可编辑且媒体文中预览，保留源码模式和未改动文本保真。
- [x] 9.5 添加图片/音频/视频「本地上传」「已有链接」菜单、光标保留、上传占位/进度/失败重试与跨页取消。
- [x] 9.6 添加 hover/focus 右下角布局/尺寸控件，支持触屏、比例保持、百分比/固定宽度、源码保存与撤销重做。
- [x] 9.7 统一阅读、编辑、历史修订媒体渲染；发布前保存当前草稿并阻止未完成上传进入发布版本。
- [x] 9.8 增加媒体 round-trip、选区/撤销、上传失败/链接过期、权限、归档不可用与播放预览测试。
- [x] 9.9 验证并实现图片内容解析到子/父 chunks 和 embedding/ES 的链路，解析失败显式标记；外链图片不自动服务端抓取。
- [x] 9.10 在上传、worker、重试和恢复重建统一拦截非图片附件索引，清除其历史 ES chunks，保留文件、元数据和页面引用。
- [x] 9.11 验证图片进入索引、音视频/PDF/Office 等附件不进入索引、旧任务不复活索引；显式导入生成的 Wiki 页面正文仍正常索引。

## 10. 编辑命令与 Markdown/HTML 导出

- [x] 10.1 接入代码块和有序列表命令，处理选区/语言/反引号围栏/续号/撤销，并使现有标题与加粗按钮可用。
- [x] 10.2 实现编辑草稿/阅读修订的导出快照契约，支持单文件 Markdown/安全 HTML 和文件名清理，不隐式发布。
- [x] 10.3 实现带本地媒体的授权 ZIP 导出：SDK 有界读取、assets 去重、相对路径和必要样式；外链保留且不主动下载。
- [x] 10.4 添加工具栏格式菜单、打包提示、大小/超时/资源失败反馈和临时资源清理。
- [x] 10.5 验证导出草稿未保存文字、布局尺寸、音视频、离线相对媒体资源、异常失败与权限，不把签名 URL 写入持久产物。

## 11. 整体验收与交付

- [x] 11.1 执行后端相关单元/契约测试、现有离线验证、前端 typecheck/test/build，修复本变更引入的失败。
- [x] 11.2 在可丢弃 MySQL/ES 环境验收双路真实排名、QA 完整恢复链、归档即时消失、恢复重建和过期批量物理清理。
- [x] 11.3 使用真实内容中心验收三类媒体上传/预览/拖动/导出，记录平台不支持的格式及具体错误。
- [ ] 11.4 浏览器对照用户原型验收侧面板/会话页时间线、回收站两次确认、文中媒体布局与导出，保存验收记录。（按用户指示跳过，交由视觉模型执行；离线等价覆盖见 acceptance-record.md）
- [x] 11.5 更新配置/API/SSE/日志字段、保留期与迁移部署/回退说明；记录与旧未归档规格的最终协调清单。

## 12. 浏览器反馈：编辑器与附件

- [x] 12.1 删除独立编辑/纯文本切换，只保留源码块编辑和预览；修复预览短链 JSON 被当作图片地址的问题。
- [x] 12.2 源码内插入位置渲染媒体/上传卡片，修复 hover 控件不可点击，图片默认居中 50% 且不超容器。
- [x] 12.3 增加普通附件上传、文件名/大小卡片和授权下载，保持非图片附件不进入索引。
- [x] 12.4 工具栏下拉统一使用图标库；代码块先选语言并使用 highlight.js 高亮。

## 13. 浏览器反馈：草稿、发布与历史

- [x] 13.1 将草稿拆为独立可覆盖工作副本；编辑时加载、保存后可跨页面继续，多次保存不增加版本，阅读与检索仍只使用已发布修订。
- [x] 13.2 发布增加说明弹窗；空说明由模型按首次内容或发布差异生成。
- [x] 13.3 历史点击只读预览旧版本，查看/比较不创建修订；显式恢复创建当前草稿。

## 14. 浏览器反馈：范围与导航

- [x] 14.1 删除共享空间、全局搜索页面路由和导航入口，删除聊天浮窗关闭按钮。
- [x] 14.2 实现知识库/Wiki 三列树形多选器、折叠已选分组与计数。
- [x] 14.3 将选中范围随会话请求发送，并在服务端与实时授权范围求交后贯穿两路检索。

## 15. 第二轮联调

- [x] 15.1 运行前后端测试与构建，修复回归。
- [ ] 15.2 在已登录浏览器逐项验收编辑、预览、附件、历史、发布、导航和检索范围。

## 16. 第三轮浏览器反馈：媒体与编辑区域

- [x] 16.1 让编辑器空白内容区可接管焦点，媒体-only 文档保留前后插入点，点击空白区可继续输入。
- [x] 16.2 将外部/受控媒体统一为加载、成功、失败状态，增加 10 秒超时和错误提示。
- [x] 16.3 失败媒体提供 Lucide 删除操作，删除只改对应源码范围并可继续编辑。
- [x] 16.4 预览和阅读的受控图片先渲染加载占位卡，新增图片默认居中、自动宽度按 50% 渲染，补充 35 项前端回归验证。

## 17. 第四轮浏览器反馈：图片解码与布局稳定

- [x] 17.1 短链解析后继续保留原位加载卡，图片脱离布局并保持隐藏，完整 decode 后一次性显示，避免占位消失、正文双跳和渐进露出。
- [x] 17.2 多行正文 textarea 在挂载和内容更新时按 scrollHeight 自动展开，无需先输入回车触发。

> 详细证据见 `implementation-notes.md`（1.x 基线）与 `acceptance-record.md`（11.x 验收，含 11.2/11.3 的离线等价验证与待可丢弃环境实测项、11.4 的视觉验收交接说明）。
