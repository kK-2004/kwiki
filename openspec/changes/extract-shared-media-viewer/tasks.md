## 1. 组件库基础

- [x] 1.1 在 ui-components 确认 Plyr 锁定版本、许可证、全屏选项和样式导入方式，记录选型验证结果。
- [x] 1.2 新增 KMediaViewer 独立子入口、类型和构建输出，避免依赖其他组件的总入口及全局 CSS。
- [x] 1.3 实现图片加载/解码/超时/重试与稳定占位，支持尺寸元数据、默认居中和 auto 50%。
- [x] 1.4 实现音视频分支懒加载 Plyr 与 CSS，完成源切换、缓冲状态、取消任务和销毁。
- [x] 1.5 实现可配置水印、含水印容器全屏与 PiP 降级，覆盖音频卡片。
- [x] 1.6 实现下载交互开关、下载回调和 source resolver；补充 API 示例及无法阻止 F12 提取的边界说明。
- [x] 1.7 按原型重设计视频展示：深色 16:9 播放区、标题层、控制栏、倍速设置和单点动态水印。
- [x] 1.8 修复组件文档的音频布局、下载按钮定位和 Basic Usage 自包含渲染。
- [x] 1.9 将图片下载入口迁移到可缩放大图模态框，支持键盘关闭、水印覆盖和下载权限。
- [x] 1.10 修复音视频已解码出帧但懒加载初始化漏接就绪事件后仍触发超时的问题。
- [x] 1.11 支持调用方显式固定组件宽高，媒体加载后仅在容器内适配且不改变组件尺寸。
- [x] 1.12 修复大图模态框的垂直居中，并保证放大溢出后仍可完整滚动查看。

## 2. 组件库验证与依赖交付

- [x] 2.1 验证慢图解码、错误、超时、重试和切换源竞态；验证水印参数与下载开关。
- [x] 2.2 构建可消费包，检查子路径 JS、类型、CSS 的真实存在与无关依赖隔离，再提供给 kwiki 验证，不自动发布 npm。

## 3. kwiki 源码修复

- [x] 3.1 修复完整 audio/video 节点扫描范围与序列化转义，覆盖换行属性及 source 子节点。
- [x] 3.2 添加严格限定的历史重复闭合标签兼容处理，保留代码/转义/无关文本，只在明确保存时持久化。
- [x] 3.3 添加连续五次布局调整、保存重开、音视频往返与代码反例的回归测试。

## 4. kwiki 集成

- [x] 4.1 引入可复现组件依赖与锁文件，使用独立子路径动态导入，编写 attachment:// URL 适配器。
- [x] 4.2 将源码卡片、预览和阅读页迁移到共享组件，保留删除/布局工具、可移植导出和现有附件索引策略。
- [x] 4.3 回归多行源码初始高度、失败媒体删除及 hover 工具可操作性，避免此前已修问题复发。

## 5. 联调验收

- [x] 5.1 运行两仓库类型检查、相关测试和生产构建，记录结果。
- [ ] 5.2 浏览器验证上传/链接音视频只有一个播放器且无闭合标签残留，验证源码/预览/阅读一致。
- [ ] 5.3 慢网验证图片解码前一直占位且不渐进露出；验证未知尺寸无坍塌回弹及错误/超时恢复。
- [ ] 5.4 在 Chromium 和 Safari 验证水印、全屏及下载开关；不将网络内容不可提取作为通过标准。
- [ ] 5.5 以生产构建与 Network 对照无媒体、仅图片、音视频页面，证明懒加载和未使用依赖不加载，记录实际 chunk 大小。
