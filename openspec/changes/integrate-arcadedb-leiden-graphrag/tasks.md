## 1. 接入边界、配置与能力契约

- [ ] 1.1 定义图抽取、图存储、社区发现、摘要、社区检索和快照注册端口及 DTO，固定首版算法模式为 `ARCADEDB_NATIVE_UNWEIGHTED`；隔离 ArcadeDB RID/SQL/Cypher，不增加 Python worker、加权算法或数据库部署依赖。
- [ ] 1.2 增加 `kwiki.graph`、`kwiki.external.arcadedb.*` 配置绑定与校验，支持独立服务 endpoint/database、读/构建凭据、TLS、连接并发及临时库前缀；补充 `KWIKI_ARCADEDB_*` 环境占位符，默认关闭且无默认地址/密码兜底。
- [ ] 1.3 实现 ArcadeDB HTTP 适配器与固定参数化命令，区分连接 3s、在线查询 1.5s、批写 30s、算法 15min 超时；提供可用于后续任务的取消/状态探测接口及脱敏错误分类。
- [ ] 1.4 实现健康、服务版本和 Leiden 输入/输出能力探测，记录目标服务兼容版本要求；未知版本、能力缺失或建库权限不足时禁用构建，不降级到正式全图执行算法。
- [ ] 1.5 使用 HTTP stub 验证关闭开关时零外拨、启用但配置不完整、认证失败、超时和能力缺失；默认测试不得依赖真实 ArcadeDB 或 Docker，真实能力验证放在第 14 组专用环境验收。

## 2. 持久化身份、元数据与迁移

- [ ] 2.1 定义包含资源/修订/生命周期、Chunk 流水线、chunkKey 和内容 hash 的 sourceChunkId，以及 entityLinkingVersion 和完整社区身份；验证同 chunkKey 不同内容/流水线不会碰撞。
- [ ] 2.2 在实施时重新枚举全部 Flyway 迁移并分配后续版本，新增实体注册/抽取结果、独立目标任务和内容/安全 epoch 存储；不修改历史迁移，运行现有迁移清单校验。
- [ ] 2.3 新增批次、按库 run、来源清单、图快照、COMMUNITY 索引版本、发布配对、审计及资源引用的持久化结构；区分 chunkIndexVersion、communityIndexVersion、graphVersion 与 mapping 版本。
- [ ] 2.4 实现每库活动 run、每日调度幂等键、抽取/回填幂等键和发布配对唯一性，以及租约、fencing、检查点与版本分配的仓储操作。
- [ ] 2.5 实现 `GraphPublication(kbId, chunkIndexVersion)` 的条件更新与发布记录，使候选 Chunk 版本配套图能够独立注册而不覆盖线上 Chunk 版本的配对。
- [ ] 2.6 补充迁移结构、唯一性、并发版本分配和持久化状态转换的验证；真实数据库迁移契约仅使用明确配置的专用测试库，不清理共享业务数据库。

## 3. ArcadeDB schema 与图投影存储

- [ ] 3.1 实现独立于 MySQL Flyway 的 ArcadeDB schemaVersion 和可重入初始化，创建 Document、Chunk、Entity、RelationEvidence、Community 及设计约定的结构/语义边类型。
- [ ] 3.2 建立实体唯一键、社区成员复合索引、Chunk 来源索引和关系证据幂等索引，所有访问强制绑定 kbId/graphVersion；提供查询计划诊断入口。
- [ ] 3.3 实现按稳定业务键批量写入来源、实体、关系和证据，以及批次检查点；批写同时受 500 条和 5 MiB 上限约束，重复来源不得累加 supportCount。
- [ ] 3.4 实现快照写入阶段与冻结边界，READY/已发布图不可被新任务覆盖；按 run/fencing 隔离过期 worker 的候选输出。
- [ ] 3.5 覆盖跨库/跨版本隔离、批次重复投递、关系多来源去重及旧快照不变性，确认正式 MENTIONS 可用于离线校验而非在线种子反查。

## 4. 实体关系抽取与内容生命周期

- [ ] 4.1 定义抽取 JSON Schema、受控实体/predicate 枚举及版本化 prompt，接入现有模型客户端；校验端点、sourceRefs、引文位置和父上下文的真实来源，拒绝虚构事实/引用及任意 schema 操作。
- [ ] 4.2 实现知识库内实体注册和保守消歧，使用规范名称、类型、已验证别名及上下文；歧义实体保留不同 ID，映射结果按来源及 entityLinkingVersion 固定复用。
- [ ] 4.3 从持久化内容事件消费抽取任务，以对应 CHILD 已成功产出为前提，排除未发布草稿；处理重启后的事件缺口，不使用一次性进程内回调作为唯一投递方式。
- [ ] 4.4 持久化抽取结果、图写入与 ES 回填独立目标状态，实现重试/接管；同来源缓存命中不重复调用模型，图故障不回滚页面发布或已有 Chunk 索引成功状态。
- [ ] 4.5 将修订更新、归档、删除、恢复及授权变化接入内容/安全 epoch 与图失效流程；撤销来源贡献，保留其他来源支持的关系，用生命周期 fencing 阻止旧任务复活资源。
- [ ] 4.6 验证虚构引文、同名歧义、重复事件、发布后崩溃、单目标成功后崩溃以及归档/恢复乱序，确保缓存、支持数和来源身份正确收敛。

## 5. ES CHILD 实体字段与异步回填

- [ ] 5.1 在现有版本化 Chunk mapping/构建配置中增加 sourceChunkId、keyword 多值 entityIds、entityLinkingVersion 和 entityLinkingStatus；通过新物理版本启用，保持旧严格 mapping 与旧索引可检索。
- [ ] 5.2 更新 CHILD 文档生成及普通索引覆盖行为：初始 PENDING，复用匹配映射或持久化安排回填；PARENT 不聚合 entityIds，Chunk 不冗余 communityId/graphVersion。
- [ ] 5.3 实现 ES 实体字段条件回填，仅写指定物理索引且校验完整来源/生命周期/映射版本；entityIds/version/READY 同步更新，不 upsert 已删除 Chunk，不覆盖正文或向量。
- [ ] 5.4 扩展 BM25/vector 字段投影、ChunkHit、ChildEvidence 及融合传递，保留实体与来源元数据；区分 READY 空数组、PENDING、FAILED、旧索引字段缺失，禁止跨代际合并。
- [ ] 5.5 将 entityLinkingVersion 纳入 Chunk 配置支持检查、重建及切换验证；映射代际改变时创建新索引，增加回填覆盖/一致性检查供图发布使用。
- [ ] 5.6 验证旧索引兼容、回填与新修订竞争、目标被删除、全量覆盖后字段恢复及单纯重跑 Leiden 不重写 entityIds；确认实体元数据不自动暴露给模型或用户。

## 6. 构建批次、固定版本与来源清单

- [ ] 6.1 实现 ALL/KNOWLEDGE_BASE 批次创建，固定提交时的有效知识库集合；默认解析当前 Chunk 物理版本，允许选择已构建且配置未脏、支持实体 schema 的候选版本。
- [ ] 6.2 为批次分配独立 COMMUNITY 版本并创建按库 run，持久化 CHUNK/COMMUNITY 双版本及各库物理目标；所有子阶段、回填、重试和审计继承该版本对，不跟随别名变化。
- [ ] 6.3 在权威数据库一致性读中生成分页持久化来源清单，绑定修订、流水线、epoch 和连续已消费事件水位；缺失 Chunk 显示 WAITING_FOR_CHUNKS，不支持的版本显示 UNSUPPORTED。
- [ ] 6.4 实现阶段协调和恢复：QUEUED、抽取、投影、聚类、摘要、索引、校验、READY，以及失败/取消/STALE；原来源重试复用 run，来源变更创建新批次和新版本。
- [ ] 6.5 将图构建引用接入 Chunk 索引管理后端，拒绝删除、原地重建或编辑被活动任务引用的目标；不阻断合法内容更新和读别名切换。
- [ ] 6.6 实现子任务结果聚合及 PARTIAL_FAILED，单库失败不阻断其他库；验证提交范围固定、目标版本切换期间重试不漂移、事件水位缺口和失败项恢复。

## 7. 隔离投影与内置无权 Leiden

- [ ] 7.1 为每个 run 在同一 ArcadeDB 服务中创建受控临时库，记录所有权和待清理资源；只写冻结来源清单中的 Entity 与 CONNECTED，不包含其他库/版本或文档结构顶点。
- [ ] 7.2 实现无权投影归一化：受控语义 predicate、无序实体对去重、移除自环、保留孤点；重复来源、双向关系和多 predicate 不形成额外权重。
- [ ] 7.3 实现 `algo.leiden('CONNECTED', maxIterations, resolution)` 调用，默认 10/1.0；记录引擎/算法版本、输入 hash 和参数，持久化临时 RID 到 entityId 的映射及算法结果。
- [ ] 7.4 校验每个输入实体恰有一个社区结果、孤点归属和空图合法性，写入新快照的 Community/成员属性/IN_COMMUNITY；拒绝缺失、额外或不一致成员，不覆盖旧快照。
- [ ] 7.5 实现算法超时/取消后的远端完成确认：未知状态进入 NEEDS_ATTENTION 并保留槽位，确认结束才清理临时库；验证重试复用已完整算法结果、不要求社区数字标签跨次稳定。

## 8. 社区摘要与 COMMUNITY 索引

- [ ] 8.1 实现按度数和独立来源支持数选择代表实体/关系/Chunk，稳定 ID 决定平局顺序；执行 20 实体、30 关系、8 Chunk、12,000 字符输入上限，不加载整个社区到模型。
- [ ] 8.2 实现结构化社区摘要及 sourceRefs 校验，输出 title/summary/keywords/coreConcepts/importantRelations/questionTypes；拒绝虚构实体、关系和引用，空图不调用模型生成摘要。
- [ ] 8.3 实现 COMMUNITY 版本注册、配置快照和严格 mapping，按 `kwiki-communities-v{communityIndexVersion}-kb{kbId}` 建索引；区分该 ES 版本与 graphVersion、mappingSchemaVersion。
- [ ] 8.4 完成摘要 embedding、批量写入、refresh 与读回验证，固定模型/维度/prompt 身份；失败社区或错误向量维度阻断完整快照发布，不静默跳过。
- [ ] 8.5 验证摘要输入截断、无效引用、孤立实体、空图、模型重试及同批次多库物理隔离；COMMUNITY 索引不得进入原 Chunk parser 重建入口。

## 9. 快照校验、发布、回滚与清理

- [ ] 9.1 实现持久化校验报告：来源覆盖、成员完整、图/抽取/ES entityIds 一致、实体字段 READY、摘要与向量完整、mapping/配置身份及刷新可读性；任何硬失败阻止 READY。
- [ ] 9.2 实现发布事务：按库持锁并锁定 epoch 行，复核来源/安全 epoch 与预期旧指针，以单次 MySQL CAS 更新 `(kbId, chunkIndexVersion)` 配对，不执行跨存储伪原子切换。
- [ ] 9.3 实现请求级快照 pin、物理索引解析和读取租约；多库请求固定各自配对，服务中途不得重新读取别名或混合其他图版本补齐结果。
- [ ] 9.4 实现默认手动发布及显式 autoPublish 开关，自动发布复用同一门禁；预建候选 Chunk 版本配套图不得切换 Chunk 别名或替换其他 Chunk 版本的图配对。
- [ ] 9.5 实现历史快照回滚、至少当前/上一版本保留和退役宽限期；清理先置 DELETING，再确认无发布/构建/读取引用，按资源清单幂等删除，部分失败保持可恢复状态。
- [ ] 9.6 验证发布前后崩溃、并发内容/权限变化、旧请求跨发布继续读取、过期摘要回滚禁用及清理与新 pin 竞争，确保任务/请求均不会混版。

## 10. 全来源权限门禁与失效处理

- [ ] 10.1 基于现有授权和来源清单实现全来源覆盖证明，包含知识库当前全部有效可索引资源及快照来源，并检查本次用户选择范围；角色名、kbId 或代表 Chunk 可读不能替代证明。
- [ ] 10.2 在社区 TopK 和种子提取之前按库执行门禁；部分不可见或范围不完整时关闭社区搜索、摘要和全部图扩展，沿用 Chunk/QA/PARENT 流程，多库请求独立处理。
- [ ] 10.3 将 content/security epoch 失效与现有 run 出站守卫接通，摘要过期即禁用；入模、遍历、引用及 SSE 再核验当前来源，权限撤销不得作为普通服务故障降级继续输出。
- [ ] 10.4 验证 PRIVATE/指定成员/库外共享、只选一页、旧快照外新增私有文档、多库混合权限及流式撤权；对被门禁拒绝的库断言社区搜索和图调用次数均为零，轨迹不泄露其内容/数量。

## 11. 在线双路召回、图扩展与 QA 集成

- [ ] 11.1 实现通过门禁后的社区 BM25/vector 并行召回及 RRF k=60，各分支 Top10、全请求最多 3 个社区；查询向量缓存按 query/model/dimensions 隔离，区分零命中与分支失败。
- [ ] 11.2 实现直接从 READY ES entityIds 提取最多 8 个种子，校验来源和映射代际；缺失/失败/空集合不回查 MENTIONS、不在线抽实体，后续批量验证种子及来源属于固定图快照。
- [ ] 11.3 实现以种子唯一索引起步的社区约束遍历，最多 2 hop、每节点检查 50 边、全 run 检查 500 边、返回 20 实体/30 关系；过滤掉的边也计入检查量，不先加载整个社区后裁剪。
- [ ] 11.4 实现允许的回退：完整授权库的社区零命中/服务故障或种子无交集可 seed-only；只有社区命中时先验证代表实体原文，无安全种子跳过增强，不为部分授权库启用回退。
- [ ] 11.5 将图关系来源转换为当前有效 CHILD，按稳定顺序形成图列表，与原 Chunk 融合列表用 RRF 去重合并；最多新增 5 个 CHILD，辅助文本最多 4,000 字符且计入既有阶段总预算，移除无保留原文支撑的断言/引用。
- [ ] 11.6 接入 `LangGraphAgenticWorkflow` 实际 CHILD 检索阶段，保持生成、QA、PARENT 补充与恢复顺序；DIRECT 不查图，es_search 契约不变，各轮共享 1.5s 增强时间及访问预算，不向模型开放任意图 DSL/范围参数。
- [ ] 11.7 验证图/社区单支失败保留 Chunk、全部 Chunk 基础设施失败保持 retrieval-failed、只有摘要不能回答、来源失效无法桥接、回调不重复加分和多轮预算不重置。

## 12. 调度、资源限额与可观测性

- [ ] 12.1 实现每天 `0 0 2 * * *`、Asia/Shanghai 的持久化调度，日期唯一键防多实例重复；当日漏跑最多补一次，有未完成批次先恢复并记录 SKIPPED_ACTIVE，默认关闭阈值触发。
- [ ] 12.2 通过 common SDK 分布式锁、数据库活动 run 约束、租约和 fencing 实现每库互斥及全局一个构建/算法槽位；手动与定时任务共用入口，失锁旧 worker 不得推进权威状态。
- [ ] 12.3 实现可配置容量校验：每库 50,000 实体、250,000 语义关系、1,000,000 来源、5,000 摘要，实体 20,000/关系 100,000 预警；超限明确失败，不删孤点或截断来源伪装成功。
- [ ] 12.4 实现离线模型共享限流默认 2 并发、1 请求/秒、单项总尝试 3 次，以及批写条数/字节限制和批次超过 6 小时预警；离线额度与在线回答分离。
- [ ] 12.5 接入构建/回填覆盖率、阶段耗时、版本配对、成本、失败与清理积压指标，以及 ES 种子提取/图扩展/证据验证轨迹；普通日志脱敏，不输出私密实体、正文和凭据。
- [ ] 12.6 使用可控时钟及故障注入验证跨日调度、漏跑补偿、手动/定时碰撞、容量阈值、远端未知状态和失锁接管，不依赖等待真实 02:00。

## 13. 管理 API 与现有后台页面

- [ ] 13.1 新增图批次/子任务、服务/调度状态、校验、重试/取消、发布/回滚和清理 API；沿用 ROLE_ADMIN、JWT、幂等命令和审计，长操作返回 batchId/runId，响应不含凭据。
- [ ] 13.2 扩展现有 ES 管理查询接口的 CHUNK/COMMUNITY 分类，提供社区版本下各库物理索引、配套 Chunk/图版本、数量、存储量、模型/mapping 配置及允许操作，不破坏原 Chunk 接口契约。
- [ ] 13.3 在 `admin-frontend` 现有 `/admin/` 索引管理中加入类型切换与 COMMUNITY 版本/物理索引视图，保留 CHUNK 创建、构建、补齐、切换和清理功能。
- [ ] 13.4 增加全量/指定知识库任务表单、Chunk 目标版本选择及父子任务详情；列表、阶段、重试、校验、审计均展示 CHUNK/COMMUNITY 双徽标，纯 Chunk 任务的 COMMUNITY 显示“不涉及”。
- [ ] 13.5 增加服务健康、每日 02:00 调度/下一次执行、容量、回填覆盖、模型成本和失败状态展示；复用持久化状态与有退避轮询，刷新后恢复任务进度。
- [ ] 13.6 验证管理 API 越权拒绝和幂等结果，补充后台测试覆盖单库/全量、双版本不漂移、部分失败、刷新恢复、危险操作禁用及原 CHUNK 管理回归。

## 14. 集成验收与运行文档

- [ ] 14.1 汇总前述离线契约测试并完成项目适用检查：迁移清单、无 Docker/密钥/架构审计、后端 compile/test-compile 及相关测试、前端与管理后台 typecheck/test；区分既有失败与本次引入的问题。
- [ ] 14.2 增加可显式启用的 ArcadeDB/ES/MySQL 专用环境契约验收，锁定实际服务版本并验证原生 Leiden 调用、两知识库/版本隔离、孤点/空图、临时库清理及 schema 重入；未配置环境时明确跳过，不伪报已验收。
- [ ] 14.3 在专用环境检查实体/成员索引 EXPLAIN 与高扇出遍历真实访问量，验证 1～2 hop 预算在底层生效；对接近初始容量上限的构建记录内存、磁盘、耗时和模型成本，形成上线限额依据。
- [ ] 14.4 完成端到端演练：页面/附件发布→抽取→ES entityIds→离线 Leiden→社区摘要→校验发布→原文引用问答，包含无权用户原流程、全量/单库任务及 CHUNK/COMMUNITY 版本配对。
- [ ] 14.5 完成跨存储部分成功、构建中 Chunk 别名切换、进程崩溃恢复、权限/生命周期变化、回滚、退役清理与图服务中断演练，确认旧请求不混版且图故障不阻断有效 Chunk 回答。
- [ ] 14.6 编写独立 ArcadeDB 服务接入配置、凭据权限、02:00 调度、后台双索引操作、限额调整、失败恢复及灰度/回滚说明；记录 Spring 事务自调用、Redisson 续期及普通问题的影子评估结果，外部加权 worker 明确列为后续范围。
