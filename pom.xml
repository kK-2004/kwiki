<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.6</version>
    <relativePath/>
  </parent>

  <groupId>com.kwiki</groupId>
  <artifactId>kwiki</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>kwiki</name>
  <description>Enterprise Wiki with agentic RAG retrieval over operator-provided middleware</description>

  <properties>
    <java.version>21</java.version>
    <jjwt.version>0.11.5</jjwt.version>
    <content-center-sdk.version>0.1.3</content-center-sdk.version>
    <kk-common.version>0.1.2</kk-common.version>
    <tika.version>2.9.1</tika.version>
    <commonmark.version>0.24.0</commonmark.version>
    <archunit.version>1.3.0</archunit.version>
    <langchain4j.version>1.20.0</langchain4j.version>
    <langgraph4j.version>1.8.20</langgraph4j.version>
    <json-schema-validator.version>2.0.0</json-schema-validator.version>
  </properties>

  <!-- 私有 GitHub Packages 仓库。认证来自 Maven settings：
       id 与下方仓库 id 相匹配的 <server>，并配以具备
       read:packages 权限的 GitHub token。同一个 token 可通过
       ${env.GITHUB_TOKEN} 同时支撑两个 server 条目。仓库 id 唯一；本仓库中
       绝不存储任何凭据。
         github        -> kK-2004/kk-common  （共享的 kk-common SDK）
         github-kfile  -> kK-2004/kFile      （内容中心附件 SDK） -->
  <repositories>
    <repository>
      <id>github</id>
      <name>GitHub kK-2004/kk-common Packages</name>
      <url>https://maven.pkg.github.com/kK-2004/kk-common</url>
    </repository>
    <repository>
      <id>github-kfile</id>
      <name>GitHub kK-2004/kFile Packages</name>
      <url>https://maven.pkg.github.com/kK-2004/kFile</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>dev.langchain4j</groupId>
      <artifactId>langchain4j-open-ai</artifactId>
      <version>${langchain4j.version}</version>
    </dependency>
    <dependency>
      <groupId>org.bsc.langgraph4j</groupId>
      <artifactId>langgraph4j-core</artifactId>
      <version>${langgraph4j.version}</version>
    </dependency>
    <dependency>
      <groupId>com.networknt</groupId>
      <artifactId>json-schema-validator</artifactId>
      <version>${json-schema-validator.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <!-- 链路追踪上下文：基于 Brave 的 Micrometer Tracing。W3C traceparent 的
         服务端抽取与客户端注入依托于 actuator 自动配置的观测过滤器；
         桥接层只负责提供 tracer。 -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-tracing-bridge-brave</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>
    <dependency>
      <groupId>com.kk</groupId>
      <artifactId>content-center-sdk</artifactId>
      <version>${content-center-sdk.version}</version>
    </dependency>
    <!-- 跨项目共享工具集：TransDTO 响应信封、BusinessException +
         GlobalExceptionHandler、RedisUtil 旁路缓存辅助类，以及基于 Redisson 的
         DistributedLock 工厂。通过 META-INF/spring/...AutoConfiguration.imports 激活，
         并由 kk.common.*.enabled 功能开关把关（web/exception 默认开启，
         redis/redisson 默认关闭）。 -->
    <dependency>
      <groupId>com.kK-2004</groupId>
      <artifactId>kk-common</artifactId>
      <version>${kk-common.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.tika</groupId>
      <artifactId>tika-core</artifactId>
      <version>${tika.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.tika</groupId>
      <artifactId>tika-parsers-standard-package</artifactId>
      <version>${tika.version}</version>
    </dependency>
    <dependency>
      <groupId>org.commonmark</groupId>
      <artifactId>commonmark</artifactId>
      <version>${commonmark.version}</version>
    </dependency>
    <dependency>
      <groupId>org.commonmark</groupId>
      <artifactId>commonmark-ext-gfm-tables</artifactId>
      <version>${commonmark.version}</version>
    </dependency>
    <dependency>
      <groupId>org.commonmark</groupId>
      <artifactId>commonmark-ext-gfm-strikethrough</artifactId>
      <version>${commonmark.version}</version>
    </dependency>
    <dependency>
      <groupId>com.googlecode.owasp-java-html-sanitizer</groupId>
      <artifactId>owasp-java-html-sanitizer</artifactId>
      <version>20220608.1</version>
    </dependency>
    <dependency>
      <groupId>co.elastic.clients</groupId>
      <artifactId>elasticsearch-java</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.squareup.okhttp3</groupId>
      <artifactId>mockwebserver</artifactId>
      <version>4.12.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>${archunit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- 可选的验收运行，针对运维方提供的 MySQL/Redis/内容中心/Elasticsearch/千问。
         由环境变量开关控制的 *ContractTest 类通过 KWIKI_IT_* 变量激活；默认的
         ./mvnw verify 完全无需 Docker，并会跳过它们。 -->
    <profile>
      <id>external-it</id>
      <properties>
        <kwiki.external-it.required>true</kwiki.external-it.required>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>3.1.0</version>
            <executions>
              <execution>
                <phase>validate</phase>
                <goals><goal>run</goal></goals>
                <configuration>
                  <target>
                    <fail message="external-it requires KWIKI_IT_MYSQL_URL, KWIKI_IT_MYSQL_USERNAME, KWIKI_IT_MYSQL_PASSWORD and the operator-provided service endpoints (run scripts/verify-external-services.sh first)">
                      <condition>
                        <not>
                          <and>
                            <isset property="env.KWIKI_IT_MYSQL_URL"/>
                            <isset property="env.KWIKI_IT_MYSQL_USERNAME"/>
                            <isset property="env.KWIKI_IT_MYSQL_PASSWORD"/>
                          </and>
                        </not>
                      </condition>
                    </fail>
                    <echo>external-it: contract tests will run against operator-provided services.</echo>
                  </target>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
